Plexy + wood — one uniform color

- model.png: the real photographic product model.
- layers/plexy_mask.png: only the Plexy plate.
- layers/wood_mask.png: only the wood box.
- layers/plexy.png and layers/wood.png: transparent editable layers.
- layers/fixed_details.png: fixed original pixels.

The same selected color is applied independently to both material masks; the writing/flower, decoration, background and reflections remain fixed.
